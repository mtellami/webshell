### Remote Control

<!-- open pipe -->
browser <==> socket-server <==> ssh-client

#### run dev
```zsh
    pip install pipenv
```
```zsh
    pipenv sync
```
```zsh
    pipenv run python manage.py runserver
```
